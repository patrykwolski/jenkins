properties([pipelineTriggers([upstream(threshold: 'UNSTABLE', upstreamProjects: '2')])])

node {
  stage ("pull") {
      git 'https://github.com/patrykwolski/jenkins.git'
  } 
  stage ("check") {
      sh "cat README.md"
  }
}
